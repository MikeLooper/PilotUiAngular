/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OrdersDto } from '../../models/orders-dto';

export interface OrdersUpdatePut$Params {
  ApiVersion?: string;
      body: OrdersDto
}

export function ordersUpdatePut(http: HttpClient, rootUrl: string, params: OrdersUpdatePut$Params, context?: HttpContext): Observable<StrictHttpResponse<any>> {
  const rb = new RequestBuilder(rootUrl, ordersUpdatePut.PATH, 'put');
  if (params) {
    rb.header('ApiVersion', params.ApiVersion, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<any>;
    })
  );
}

ordersUpdatePut.PATH = '/orders/update';
