/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AddResponseInt } from '../../models/add-response-int';
import { SuppliersDto } from '../../models/suppliers-dto';

export interface SuppliersAddPost$Params {
  ApiVersion?: string;
      body: SuppliersDto
}

export function suppliersAddPost(http: HttpClient, rootUrl: string, params: SuppliersAddPost$Params, context?: HttpContext): Observable<StrictHttpResponse<AddResponseInt>> {
  const rb = new RequestBuilder(rootUrl, suppliersAddPost.PATH, 'post');
  if (params) {
    rb.header('ApiVersion', params.ApiVersion, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AddResponseInt>;
    })
  );
}

suppliersAddPost.PATH = '/suppliers/add';
