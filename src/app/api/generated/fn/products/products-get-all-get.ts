/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProductsDto } from '../../models/products-dto';

export interface ProductsGetAllGet$Params {
  ApiVersion?: string;
}

export function productsGetAllGet(http: HttpClient, rootUrl: string, params?: ProductsGetAllGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ProductsDto>>> {
  const rb = new RequestBuilder(rootUrl, productsGetAllGet.PATH, 'get');
  if (params) {
    rb.header('ApiVersion', params.ApiVersion, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ProductsDto>>;
    })
  );
}

productsGetAllGet.PATH = '/products/get-all';
