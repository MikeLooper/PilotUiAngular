/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProductsDto } from '../../models/products-dto';

export interface ProductsGetProductIdGet$Params {
  productId: (number | string);
  ApiVersion?: string;
}

export function productsGetProductIdGet(http: HttpClient, rootUrl: string, params: ProductsGetProductIdGet$Params, context?: HttpContext): Observable<StrictHttpResponse<ProductsDto>> {
  const rb = new RequestBuilder(rootUrl, productsGetProductIdGet.PATH, 'get');
  if (params) {
    rb.path('productId', params.productId, {});
    rb.header('ApiVersion', params.ApiVersion, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProductsDto>;
    })
  );
}

productsGetProductIdGet.PATH = '/products/get/{productId}';
