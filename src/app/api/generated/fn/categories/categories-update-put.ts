/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CategoriesDto } from '../../models/categories-dto';

export interface CategoriesUpdatePut$Params {
  ApiVersion?: string;
      body: CategoriesDto
}

export function categoriesUpdatePut(http: HttpClient, rootUrl: string, params: CategoriesUpdatePut$Params, context?: HttpContext): Observable<StrictHttpResponse<any>> {
  const rb = new RequestBuilder(rootUrl, categoriesUpdatePut.PATH, 'put');
  if (params) {
    rb.header('ApiVersion', params.ApiVersion, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<any>;
    })
  );
}

categoriesUpdatePut.PATH = '/categories/update';
