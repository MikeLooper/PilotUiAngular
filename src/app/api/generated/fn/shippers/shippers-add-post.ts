/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AddResponseInt } from '../../models/add-response-int';
import { ShippersDto } from '../../models/shippers-dto';

export interface ShippersAddPost$Params {
  ApiVersion?: string;
      body: ShippersDto
}

export function shippersAddPost(http: HttpClient, rootUrl: string, params: ShippersAddPost$Params, context?: HttpContext): Observable<StrictHttpResponse<AddResponseInt>> {
  const rb = new RequestBuilder(rootUrl, shippersAddPost.PATH, 'post');
  if (params) {
    rb.header('ApiVersion', params.ApiVersion, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AddResponseInt>;
    })
  );
}

shippersAddPost.PATH = '/shippers/add';
